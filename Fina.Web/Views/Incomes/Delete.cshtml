@model Fina.Web.Models.IncomesDeleteVm

@{
    ViewData["Title"] = "Delete";
}

<br />

<h2 class="text-center overview-title">Incomes</h2>

<br />

<div class="container simple-center-block">
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <hr />
        <table class="table-expand">
            <thead>
                <tr class="table-expand-row">
                    <th width="400">Name</th>
                    <th width="200">Work Hours</th>
                    <th>Variable</th>
                    <th class="text-right" width="150">Amount</th>
                </tr>
            </thead>
            <tbody>
                <tr class="table-expand-row" data-open-details>
                    <td>@Model.Name</td>
                    <td>@Model.WorkHours</td>
                    <td>@Model.Variable.ToString()</td>
                    <td class="text-right">€@Model.Amount</td>
                </tr>
            </tbody>
        </table>

        <br />

        <form asp-action="Delete" class="text-center">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="button" />
            <a asp-action="Index" class="button" >Back to List</a>
        </form>
    </div>
</div>

