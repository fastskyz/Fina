@model Fina.Web.Models.SecurityDeleteVm

@{
    ViewData["Title"] = "Delete";
}

<br />

<h2 class="text-center overview-title">Incomes</h2>

<br />

<div class="container simple-center-block">
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <hr />
        <table class="table-expand">
            <thead>
                <tr class="table-expand-row">
                    <th width="400">Type</th>
                    <th width="200">Monthly</th>
                    <th>Start Date</th>
                    <th class="text-right" width="150">Amount Saved</th>
                </tr>
            </thead>
            <tbody>
                <tr class="table-expand-row" data-open-details>
                    <td>@Model.Type</td>
                    <td>@Model.Monthly</td>
                    <td>@Model.StartDate.ToString()</td>
                    <td class="text-right">€@Model.Amount</td>
                </tr>
            </tbody>
        </table>

        <br />

        <form asp-action="Delete" class="text-center">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="button" />
            <a asp-action="Index" class="button" >Back to List</a>
        </form>
    </div>
</div>

